# Override built-ins with better defaults.
alias vi='nvim'
alias vim='nvim'

alias ls='ls -G'
alias la='ls -a'
alias ll='ls -l'
alias lal='ls -al'

# Function aliases for easier execution.
alias pf='fuzzyfile'
alias pd='fuzzydir'
